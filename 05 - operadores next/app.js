'use strict'

//Operadores de comparación Y Operadores lógicos

//Operadores de Comparación -> Comparan dos expresión y devuelven un valor booleano (Si es verdadero o Si es falso)

    // ==   (Son iguales)
    // != (No son iguales)
    // === (Son estrictamente iguales Identidad)
    // >= (Más grande o igual)
    // <= (Más pequeño o igual)

//Operadores lógicos -> 